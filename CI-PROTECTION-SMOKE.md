# CI protection smoke test — DO NOT MERGE

Ephemeral branch proving that the required checks on `main` actually block a
merge. This branch and its pull request are throwaway artifacts.

**This branch must never be merged.** It will be closed and deleted once the
proof is recorded.

- base: `afab7b7e5a0d2037467f6562cf04312df4cddbc5`
- required checks under test: `functions-tests`, `firestore-rules`
